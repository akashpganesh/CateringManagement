@model IEnumerable<CateringManagement.Models.MenuItem>

@{
    ViewData["Title"] = "Create Order";
    Layout = "~/Views/Shared/_CustomerLayout.cshtml";
}

<h2 class="mb-4 text-center">Create New Order</h2>

<div class="container mt-4">
    <div class="card shadow p-4 rounded-3">

        <form id="orderForm">
            <div class="row mb-3">
                <div class="col-md-6">
                    <label class="form-label fw-bold">Event Date</label>
                    <input type="date" id="eventDate" class="form-control" required />
                </div>
                <div class="col-md-6">
                    <label class="form-label fw-bold">Event Time</label>
                    <input type="time" id="eventTime" class="form-control" required />
                </div>
            </div>

            <div class="mb-3">
                <label class="form-label fw-bold">Venue</label>
                <input type="text" id="venue" class="form-control" placeholder="Enter venue" required />
            </div>

            <div class="mb-3">
                <label class="form-label fw-bold">Contact Number</label>
                <input type="text" id="contactNumber" class="form-control" placeholder="Enter contact number" required />
            </div>

            <div class="mb-3">
                <label class="form-label fw-bold">Notes</label>
                <textarea id="notes" class="form-control" rows="2" placeholder="Any additional notes"></textarea>
            </div>

            <hr />
            <h5 class="mb-3">Select Menu Items</h5>

            <table class="table table-bordered" id="menuTable">
                <thead class="table-dark">
                    <tr>
                        <th>Dish Name</th>
                        <th>Category</th>
                        <th>Price</th>
                        <th>Available</th>
                        <th>Vegetarian</th>
                        <th>Quantity</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Name</td>
                            <td>@item.Category</td>
                            <td>@item.Price.ToString("C")</td>
                            <td>@(item.Availability ? "Yes" : "No")</td>
                            <td>@(item.IsVegetarian ? "Yes" : "No")</td>
                            <td>
                                <input type="number" min="0" value="0" class="form-control quantityInput" data-menuid="@item.MenuId" @(item.Availability ? "" : "disabled") />
                            </td>
                        </tr>
                    }
                </tbody>
            </table>


            <div class="text-center mt-3">
                <button type="submit" class="btn btn-success px-4">Place Order</button>
                <a asp-action="Index" class="btn btn-secondary px-4">Cancel</a>
            </div>
        </form>

        <div id="orderMessage" class="mt-3 text-center"></div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const orderForm = document.getElementById("orderForm");
            const orderMessage = document.getElementById("orderMessage");

            orderForm.addEventListener("submit", async function (e) {
                e.preventDefault();
                orderMessage.innerHTML = "";

                // Collect order details
                const eventDate = document.getElementById("eventDate").value;
                const eventTime = document.getElementById("eventTime").value;
                const venue = document.getElementById("venue").value.trim();
                const contactNumber = document.getElementById("contactNumber").value.trim();
                const notes = document.getElementById("notes").value.trim();

                // Collect menu items with quantity > 0
                const items = Array.from(document.querySelectorAll(".quantityInput"))
                    .map(input => {
                        return { MenuId: parseInt(input.dataset.menuid), Quantity: parseInt(input.value) };
                    })
                    .filter(i => i.Quantity > 0);

                if (!items.length) {
                    orderMessage.innerHTML = '<div class="alert alert-warning">Select at least one menu item.</div>';
                    return;
                }

                const requestBody = {
                    EventDate: eventDate,
                    EventTime: eventTime,
                    Venue: venue,
                    ContactNumber: contactNumber,
                    Notes: notes,
                    Items: items
                };

                try {
                    const token = sessionStorage.getItem("token"); // JWT token
                    const response = await fetch("/api/Order", {
                        method: "POST",
                        headers: {
                            "Content-Type": "application/json",
                            "Authorization": `Bearer ${token}`
                        },
                        body: JSON.stringify(requestBody)
                    });

                    const data = await response.json();

                    if (response.ok) {
                        orderMessage.innerHTML = `<div class="alert alert-success">${data.message} Total Amount: ₹${data.totalAmount}</div>`;
                        orderForm.reset();
                        document.querySelectorAll(".quantityInput").forEach(i => i.value = 0);
                    } else {
                        orderMessage.innerHTML = `<div class="alert alert-danger">${data.message}</div>`;
                    }
                } catch (err) {
                    console.error(err);
                    orderMessage.innerHTML = `<div class="alert alert-danger">An unexpected error occurred.</div>`;
                }
            });
        });
    </script>
}
